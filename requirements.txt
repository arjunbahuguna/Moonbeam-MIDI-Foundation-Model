#torch>=2.2
torch>=2.6,<3.0

accelerate
appdirs
loralib
bitsandbytes
black
black[jupyter]
datasets
fire
peft==0.10.0
tokenizers==0.19.1
transformers>=0.0.0
# transformers>=4.30 # Added because when we run finetuning script with torchrun, the error is: tokenizers>=0.19,<0.20 is required for a normal functioning of this module, but found tokenizers==0.22.2. Try: `pip install transformers -U` or `pip install -e '.[dev]'` if you're working with git main
sentencepiece
py7zr
scipy
#optimum
matplotlib
gradio
chardet
openai

#typing-extensions==4.8.0
typing-extensions>=4.10.0

tabulate
matplotlib
scikit-learn
wandb